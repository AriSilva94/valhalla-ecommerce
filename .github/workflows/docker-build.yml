name: Build and push Docker image

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute lowercase image name
        id: image
        run: echo "name=${REGISTRY}/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"
        env:
          REGISTRY: ${{ env.REGISTRY }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref_name == 'main' }}
            type=raw,value=dev,enable=${{ github.ref_name == 'develop' }}
            type=sha,format=long

      - name: Compute build args for this branch
        id: buildargs
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "media_host=${{ vars.PROD_NEXT_PUBLIC_MEDIA_HOST }}" >> "$GITHUB_OUTPUT"
            echo "site_url=${{ vars.PROD_NEXT_PUBLIC_SITE_URL }}" >> "$GITHUB_OUTPUT"
            echo "app_env=production" >> "$GITHUB_OUTPUT"
          else
            echo "media_host=${{ vars.DEV_NEXT_PUBLIC_MEDIA_HOST }}" >> "$GITHUB_OUTPUT"
            echo "site_url=${{ vars.DEV_NEXT_PUBLIC_SITE_URL }}" >> "$GITHUB_OUTPUT"
            echo "app_env=staging" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_MEDIA_HOST=${{ steps.buildargs.outputs.media_host }}
            NEXT_PUBLIC_SITE_URL=${{ steps.buildargs.outputs.site_url }}
            NEXT_PUBLIC_APP_ENV=${{ steps.buildargs.outputs.app_env }}
            NEXT_PUBLIC_ANALYTICS_ENABLED=${{ vars.NEXT_PUBLIC_ANALYTICS_ENABLED }}
            NEXT_PUBLIC_ANALYTICS_MODE=${{ vars.NEXT_PUBLIC_ANALYTICS_MODE }}
            NEXT_PUBLIC_GTM_ID=${{ vars.NEXT_PUBLIC_GTM_ID }}
            NEXT_PUBLIC_GA_MEASUREMENT_ID=${{ vars.NEXT_PUBLIC_GA_MEASUREMENT_ID }}

      - name: Trigger Dokploy deploy (develop)
        if: github.ref_name == 'develop'
        env:
          WEBHOOK_URL: ${{ secrets.DOKPLOY_DEV_WEBHOOK_URL }}
        run: |
          if [ -z "$WEBHOOK_URL" ]; then
            echo "DOKPLOY_DEV_WEBHOOK_URL secret not set, skipping."
            exit 0
          fi
          curl -fsS -X GET "$WEBHOOK_URL"

      - name: Trigger Dokploy deploy (main)
        if: github.ref_name == 'main'
        env:
          WEBHOOK_URL: ${{ secrets.DOKPLOY_PROD_WEBHOOK_URL }}
        run: |
          if [ -z "$WEBHOOK_URL" ]; then
            echo "DOKPLOY_PROD_WEBHOOK_URL secret not set, skipping."
            exit 0
          fi
          curl -fsS -X GET "$WEBHOOK_URL"
